# CONTRIBUTING.md（執筆ルール）

このドキュメントは、本リポジトリを継続的に更新していく上での**自分自身への執筆ルール**です。
個人の学習記録ではありますが、後から見返した時に一貫性を保つため、また第三者が読んでも理解しやすい状態を保つために定めています。

---

## 1. 執筆の心構え

- **完璧を目指さない**：まず書き切ってからブラッシュアップする。空白のチャプターより粗くても存在するチャプターの方が価値がある
- **未来の自分が読者**：半年後の自分が読んで理解できるレベルの粒度で書く（用語の説明を省略しすぎない）
- **転職活動での閲覧を想定**：採用担当者やエンジニアが見ても恥ずかしくない最低限の体裁（誤字脱字・崩れたMarkdown）は整える

---

## 2. ファイル・フォルダ命名規則

| 対象 | 命名規則 | 例 |
|---|---|---|
| チャプターフォルダ | `0X_snake_case英語名` | `05_fine_tuning` |
| チャプター本体 | 各フォルダ直下に `README.md` を1つ | `docs/05_fine_tuning/README.md` |
| サブトピック分割時 | `0Xa_`, `0Xb_` の連番を付与 | `05a_lora_basics.md` |
| 画像・図ファイル | `内容が分かる英語名.png/svg` | `rag_architecture_overview.png` |

> 注：チャプター番号（`0X_`）は目次の並び順と一致させる。欠番・重複がないか追加時に必ず確認する。

---

## 3. Markdownスタイルガイド

- 見出しは `#`（チャプタータイトル） → `##`（大項目） → `###`（小項目）の3階層を基本とする
- 比較・整理には積極的に**表（テーブル）**を使う（箇条書きより一覧性が高い場合）
- 重要な専門用語は初出時に**太字**にし、簡潔な定義を添える
- コードブロックには必ず言語名を指定する（例：\`\`\`python）
- 図解が有効な箇所は、可能な限りテキストや画像ではなく **Mermaid記法** を優先する（GitHub上でそのままレンダリングされ、修正もしやすいため）

---

## 4. 図表・画像の扱い方

| 種類 | 保存場所 | 使用基準 |
|---|---|---|
| Mermaidで表現できる図（フロー、シーケンス等） | ファイル内に直接記述（画像化しない） | 原則こちらを優先 |
| スクリーンショット（ツールのUI等） | 該当チャプターの `assets/` フォルダ | ツールの実際の操作画面を示したい場合 |
| 手描き・外部作成の概念図 | 該当チャプターの `assets/` フォルダ | Mermaidで表現しづらい複雑な図の場合 |
| リポジトリ全体で使う画像（バナー等） | ルート直下の `assets/` フォルダ | 複数チャプターで共通利用する場合のみ |

画像を挿入する際は、相対パスで参照する：

```markdown
![RAGアーキテクチャ図](./assets/rag_architecture_overview.png)
```

---

## 5. 新規チャプター追加チェックリスト

新しいチャプターを追加する際は、以下を順に実施する。

- [ ] `docs/0X_チャプター名/` フォルダを作成する
- [ ] フォルダ内に `README.md` と `assets/` を作成する
- [ ] ルートの `README.md` の目次テーブルに行を追加する（ステータスは「作成中」から開始）
- [ ] 内容を書き終えたらステータスを「完了」に更新する
- [ ] 他チャプターと関連する内容があれば相互リンクを追加する
- [ ] Markdownの構文崩れがないか、GitHub上でプレビュー確認する

---

## 6. コミットメッセージの書き方（任意ルール）

学習記録として、後から変更履歴を追いやすくするために以下の形式を推奨する。

```
[add] 05_fine_tuning: LoRAの基本概念を追加
[fix] 03_design_patterns: RAGのMermaid図の誤りを修正
[update] README: 目次にチャプター07を追加
```

---

## 7. 定期的な見直し

- 3ヶ月に一度を目安に、既存チャプターの内容が最新の情報と乖離していないか確認する（特にモデル比較表やライブラリのバージョン情報）
- 情報が古くなった箇所には `> 【注意】2026年X月時点の情報です。最新情報は公式ドキュメントを確認してください。` のような注記を追加する

---

## 8. 他リポジトリとの相互参照時の注意

実装ポートフォリオ（AI-Recipe-Book等）のドキュメントを参照・引用する際は、以下を確認する。

- **番号体系の衝突を避ける**：参照先リポジトリが独自の「フェーズ」「バージョン」番号を持っている場合、本リポジトリのチャプター番号（`0X_`）や、学習ロードマップのフェーズ番号と意味が異なることがある。混同を招く名称（例：両方のリポジトリに独立した「フェーズ3」が存在する状態）は避け、参照先のフォルダ名・見出しに文脈が分かる接頭辞（例：`roadmap_phase3_`）を付けるなど、どちらの体系を指しているか一目で判別できるようにする
- **リンク切れの防止**：参照先のパスやファイル名を変更した場合、本リポジトリ側のリンク記述も忘れずに更新する
